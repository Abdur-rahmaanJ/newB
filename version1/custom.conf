[FILE]

filename = txt.nub

[KEYWORDS]

if1_setting = si
if2_setting = kan
then_setting = alor
endcon_setting = zis_sa
ask_setting = Dmand
store_setting = store_li
in_setting = dans
output_setting = wa_print 
use_setting = servi

[KEYWORDS_EN]

if1_setting = if
if2_setting = when
then_setting = then
endcon_setting = only
ask_setting = ask
store_setting = store
in_setting = in
output_setting = output 
use_setting = use
